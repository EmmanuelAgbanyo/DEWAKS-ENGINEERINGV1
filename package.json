{
    "name": "daew-root",
    "private": true,
    "scripts": {
        "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix webapp\"",
        "install:all": "npm install --prefix webapp && cd backend && bun install"
    },
    "dependencies": {
        "@neondatabase/serverless": "^1.0.2",
        "build": "^0.1.4"
    }
}
